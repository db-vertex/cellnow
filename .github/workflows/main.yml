on: 
  push:
   branches:
    - main
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3
    
    - name: 📂 Sync files
      uses: db-vertex/cellnow
      with:
        server:  ftp.dbvertex.com
        username: ${{ secrets.FTP_PASSWORD }}
        password: ${{ secrets.FTP_USERNAME }}
